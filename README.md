This project implements a Convolutional Neural Network (CNN) trained on the MNIST dataset to recognize handwritten digits (0–9). The trained model is deployed through an interactive Gradio web interface, allowing users to draw digits on a canvas and receive real-time predictions.

The application demonstrates the complete deep learning workflow, from model training and evaluation to deployment in a user-friendly interface.
